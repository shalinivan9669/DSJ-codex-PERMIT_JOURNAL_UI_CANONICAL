# Реальная остановка базы и перезапуск контейнеров

**PASS.** 22.09.2026, 14:47:21–14:48:08 UTC. Проверен только изолированный Compose project `demo-commercial-release` на `http://localhost:8080`, отдельный Docker daemon в WSL Ubuntu-24.04. Перед операциями сверены точные имена пяти контейнеров и их Compose project labels. Проверена политика tenant `demoOnly=true`. Docker Desktop, пользовательский сервер 3200, старые 3100/4100 и Windows PostgreSQL 55432 не затрагивались.

В новой заявке `f6873fef-385f-46c7-acd3-dc868d5cfde2` созданы четыре синтетических получателя и четыре протокола. Сервер выделил четыре номера и создал десять заданий. Непосредственно перед аварией через HTTP наблюдались два RUNNING, одно SUCCEEDED и семь PENDING. Уже опубликованный DOCX скачан и проверен по SHA до остановки.

Команда `docker stop --time 0 demo-commercial-release-db-1` действительно остановила только базу. Остановка подтверждена inspect, а журнал PostgreSQL после старта подтверждает `database system was not properly shut down; automatic recovery in progress` и WAL redo. База не подменялась заглушкой. Между подтвержденной остановкой 14:47:28.350 и запуском 14:47:42.403 прошло около 14 секунд.

В этот промежуток запросы `/api/ready`, `/api/jobs` и повторный `/api/print-requests/.../finalize` вернули **HTTP 500 SERVICE_ERROR**, сообщение «Сервис временно недоступен» и correlationId. Ни один запрос не вернул ready/success. Реальные тела ответов сохранены в `result.json`; cookie, токены, пароли и строка подключения не публикуются.

После `docker start` база выполнила recovery, worker самостоятельно продолжил очередь. **10/10 SUCCEEDED**, ручной retry не использован. Сохранились одна issuance, все четыре номера и исходный уже опубликованный artifact. Скачаны и сверены по SHA все десять файлов новой заявки. Дополнительно сохранились четыре оригинальных файла ранее выпущенного удостоверения ПБ с созданным портретом.

Затем перезапущены ровно эти пять контейнеров с сохранением их volumes:

- `demo-commercial-release-db-1`;
- `demo-commercial-release-api-1`;
- `demo-commercial-release-worker-1`;
- `demo-commercial-release-web-1`;
- `demo-commercial-release-ingress-1`.

После возвращения readiness выполнена новая аутентификация. Повторно скачаны **14 файлов**, SHA-256 полностью совпали с состоянием до перезапуска. Документные номера и единственная issuance также совпали. `inspect` подтверждает новое время старта каждого из пяти контейнеров и running=true.

Доказательства: `result.json` — переходы состояний, HTTP-ответы, IDs, номера, SHA, время и контейнеры; `db.log`, `worker.log`, `api.log` — журналы узкого временного диапазона. Воспроизводящий сценарий: `scripts/verification/resilience-http.mjs`, с обязательным `DEMO_RESILIENCE_SYNTHETIC=1`; он отказывается продолжать вне demoOnly и проверяет точный Compose project.

Это фактическое восстановление после остановки БД и перезапуска контейнеров. Перезагрузка физического хоста, отключение питания и отдельная проверка TLS в данный сценарий не входят.

# Четыре карточки и созданный портрет: реальный браузер 3200

Статус: **фото в ПТМ, ПБ и ПС — PASS; вёрстка повторного блока БиОТ — FAIL, исправление передано владельцу печати.**

Отдельная синтетическая заявка: http://localhost:3200/requests/fff50a43-a6f1-444b-bde2-39821d04ae79/edit. В ней один вымышленный получатель и четыре формы: `biot-worker-card` 14, `ptm-card` 14, `pb-card` 14 и `ps-card` 15. Данные существующих пользовательских заявок не менялись.

Браузер загрузил созданный портрет, сохранил кадр 3 × 4, проверил его сохранение после перезагрузки, выполнил проверку данных, открыл настоящий PDF-предпросмотр и оформил ровно один выпуск. Все 18 заданий завершились с первой попытки. Скачаны 18 файлов; каждый SHA-256 совпал с серверным. Шесть встраиваний фото в DOCX побайтно совпали с нормализованным PNG: три формы с фото × предпросмотр/оригинал. У исторического макета БиОТ `photo=false` — дополнительное фотополе не добавлялось.

Исходный браузерный прогон упёрся в лимит ожидания 180 секунд, рассчитанный на одну форму: на последнем снимке было готово 15 из 18 заданий. Worker продолжил работу и завершил оставшиеся три. Сохранены исходные trace и экраны в соседней папке `generated-photo-four-preview`. Последующий браузерный readback той же заявки прошёл за 7,7 с (полный запуск 10,7 с); он не создавал новый выпуск или номера. Для будущих прогонов лимит ожидания зависит от числа форм.

Вручную просмотрены семь листов оригинальных PDF и три экрана: кадрирование, PDF-предпросмотр, готовая заявка. Фото ПТМ, ПБ и ПС чёткое и находится в заданном поле, без наложений. Верхние полосы эмитента сохранены. На первом листе БиОТ нижний повторный правый блок не переносит длинное имя/должность председателя и обрезает строку справа. Верхний блок того же листа переносит её корректно. Это содержательная ошибка вёрстки, поэтому общий визуальный PASS не выставлен.

Точное доказательство дефекта: `46ad6ff3-40cd-4121-8c05-f22a3a9eaddd-biot-worker-card-a4221d3d-94f9-420f-81be-66adb7c4a881.page-1.png` и PDF с тем же началом имени. Результаты по каждому листу — `pdf-visual-review.json`; все файлы, версии шаблонов и SHA — `photo-result.json`.

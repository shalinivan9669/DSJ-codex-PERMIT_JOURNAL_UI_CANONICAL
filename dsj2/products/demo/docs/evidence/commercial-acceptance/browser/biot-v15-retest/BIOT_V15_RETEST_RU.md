# БиОТ v15: повторная проверка реального выпуска

**PASS.** Новая отдельная синтетическая заявка: http://localhost:3200/requests/6b9ab371-e8c0-4b9c-bc9b-012dc5cf0070/edit. Браузерный прогон прошёл без пропусков и повторов; полный запуск занял 81,1 с.

Использованы та же программа «Тестовая программа безопасности» и неизменённый профиль эмитента с председателем «Синтетический Председатель Әли — Председатель контрольной комиссии», на которых v14 обрезала строку. Через интерфейс выполнены сохранение, перезагрузка, проверка, настоящий PDF-предпросмотр и один выпуск. Все шесть артефактов скачаны и проверены по SHA-256. Новая неизменяемая версия шаблона — `biot-worker-card` 15, SHA `b7a1d552d67470b1900041bbca24d0e58225df73d023f7d76722644e830b5556`.

Оба листа оригинального серверного PDF просмотрены вручную. Скачанный DOCX дополнительно открыт в Microsoft Word 16.0 (build 16.0.17932), экспортирован в PDF и проверен: оба листа просмотрены. Во всех четырёх листах нижний правый блок полностью переносит строку председателя внутри рамки; программа, заголовки, повторные поля и полосы эмитента не обрезаны и не перекрыты. Словарная, геометрическая и страничная проверки Word также прошли. Временные восемь bundled Liberation Serif/Sans 2.1.5 шрифтов удалены после проверки.

Для исправления изменена геометрия только нижнего правого блока: его положение и ширина приведены к корректно переносящему текст верхнему блоку. Вертикальные разделители начинаются ниже полосы эмитента. Портрет хранится в записи получателя, но в этой исторической форме `photo=false`; фотополе в неё не добавлено. ПТМ14, ПБ14 и ПС15 с фото ранее проверены отдельно и не перевыпускались.

Перед установкой проверена пустая очередь. Создана только одна новая TemplateVersion; девять других актуальных форм сохранены. Перезапущен только API4200 для исправленного алгоритма readiness, Next3200 и worker5 не перезапускались. До/после обновления совпали SHA всех 145 заявок, 266 строк, 4 профилей, 52 документов, 15 выпусков, 11 последовательностей и 147 артефактов; все 147 существовавших файлов прочитаны и проверены. Это состояние зафиксировано до создания новой тестовой заявки.

Доказательства:

- `photo-result.json`, `pdf-visual-review.json`, `results.json`, изображения двух листов, экраны предпросмотра и готовой заявки, шесть скачанных файлов.
- `../../word/biot-v15-browser/word-results.json`, Word PDF, два изображения листов и font-session.json.
- `../user-preview-biot15-before.json`, `../user-preview-biot15-result.json`, `../user-preview-biot15-ready.json` и журнал API.
- Каноническое подтверждение 20/20 актуальных DOCX: `../../word/final-active.json`, связанное с `printing/word-final-biot-v15/manifest.json` по точным SHA.

Исходный дефект v14 сохранён в `../generated-photo-four-preview-readback` и `../../word/generated-photo-four`. Его исторический FAIL не переписан в PASS; этот отчёт фиксирует результат исправленной версии и новой заявки.

# Созданный портрет в удостоверении ПБ

**PASS.** Реальный браузерный сценарий на `http://localhost:8080`, Chrome 152.0.7977.83, завершен за 38,5 с. Создана отдельная синтетическая заявка; существующие данные и пользовательский сервер `localhost:3200` не менялись.

Заявка: [ТЕСТ · созданный портрет · pb-card](http://localhost:8080/requests/46f15190-eb04-4dc9-a7dd-81fc619390a8/edit). Форма ПБ v11, SHA-256 `a12d971728f5e7568e295386d2a0061be32d30b7279222f8059fbb4b9d965379`.

Через интерфейс загружен портрет вымышленного человека, созданный встроенным генератором изображений. SHA-256 исходного PNG: `82f3d34ad38f13a9cf9511d63dc5ab9b9acb19f12a5816c3a1e1b5669e907484`. Открыт диалог кадрирования, сохранено фото, затем страница обновлена. Сохраненное фото осталось у получателя. Данные проверены; открыт реальный PDF предпросмотра; выполнен тестовый выпуск.

Скачаны и проверены по серверному SHA все шесть артефактов: DOCX/PDF предпросмотра, оригинальные DOCX/PDF, ZIP и XLSX. Нормализованное фото `c171d3030dce69d0bd10191c7fad7888096d24def53ff47bc0827d719ff1da76` обнаружено в **обоих DOCX байт-в-байт**, а не только по имени файла. Фотография показана и в серверном PDF. После этого скачанный оригинал дополнительно открыт в Microsoft Word и экспортирован в PDF: две страницы, сохранены все слова, размеры совпали с серверным PDF.

Вручную просмотрены `portrait-crop.png`, `pdf-preview.png`, `issued-portrait.png`, обе страницы оригинального серверного PDF и обе страницы его Word-экспорта. Портрет читаемый, кадр 3:4; фото не перекрывает имя или другие данные. Полоса компании внутри рамки, текст не обрезан. Вторая страница с повторными проверками также просмотрена.

Основной машинный результат: `photo-result.json`; результат Playwright: `results.json`; Word: `../../word/generated-photo-pb/word-results.json`. Проверки TypeScript и ESLint нового сценария прошли. Пароли и cookie в отчеты не включены.

Это тестовая выдача с пометкой ДЕМО; физическая печать здесь не проверялась. Сценарий воспроизводится `apps/web/e2e/generated-photo.spec.ts` и принимает путь к явно созданному тестовому портрету через `DEMO_E2E_PORTRAIT`.
